usage: {{ lobster_app_name }} <route> [<args>, <flags>, <params>]

{{ lobster_color_echo('notice', 'Routes') }}
{% for route,description in routes %}
    {{ route|replace({'.':' '}) }}
{% endfor %}

Calling `{{ lobster_app_name }}` without a command is equivalent to `example_app {{ lobster_default_route }}`.
This can be configured using the variable $lobster_default_route.
