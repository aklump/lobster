#!/bin/bash
#
# @file
# Your app's default configuration file; this is installed in an app instance during during init (if using core's init route).

example_app_alpha="Aaron"
example_app_bravo="Brian"
