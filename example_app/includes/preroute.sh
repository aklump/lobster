#!/bin/bash
#
# @file
# Pre-route is automatically called every time example_app.sh is executed, after bootstrap and before the route is executed.  The path to the route file is availailable as $lobster_route.  The operation as available as $lobster_op.  Note that you can also create preroute.{route_id} if you want.
